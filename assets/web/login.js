loginBtn.onclick=async()=>{loginBtn.disabled=true;loginResult.textContent='Verificando…';const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:username.value,password:password.value})});const j=await r.json();loginBtn.disabled=false;if(!r.ok){loginResult.textContent=j.detail||'No se pudo iniciar sesión';return}location=window.ELEGANCE_NEXT||'/admin'};password.addEventListener('keydown',e=>{if(e.key==='Enter')loginBtn.click()});
