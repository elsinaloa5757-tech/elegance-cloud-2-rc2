setupBtn.onclick=async()=>{setupBtn.disabled=true;setupResult.textContent='Creando acceso…';const r=await fetch('/api/auth/setup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:username.value,displayName:displayName.value,password:password.value})});const j=await r.json();setupBtn.disabled=false;if(!r.ok){setupResult.textContent=j.detail||'No se pudo crear la cuenta';return}setupResult.textContent='Propietario creado. Abriendo acceso…';setTimeout(()=>location='/login',700)};
